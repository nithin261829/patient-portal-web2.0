# Local Development Overrides
# Copy this file to .env.local for local development overrides
# .env.local is git-ignored

# Most configs are hardcoded in src/config/environment.ts
# Only override these if you need different values locally:

# API Configuration (defaults to localhost:4300 in dev)
# VITE_API_URL=http://localhost:4300
# VITE_CONF_URL=http://localhost:4300
# VITE_API_KEY=

# Tenant Configuration (defaults to brightsmiledental in dev)
# VITE_TENANT_ID=brightsmiledental
# VITE_ORG_ID=

# Google Maps API Key (only if you need a different key)
# VITE_GOOGLE_MAPS_API_KEY=
